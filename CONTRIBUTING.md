# Contributing

## Principios

1. **Specs first** — todo código nuevo debe estar especificado en `specs/` antes de implementarse.
2. **Core intocable** — ninguna contribución puede modificar `src/core/` sin revisión de arquitectura.
3. **Plugins autocontenidos** — cada plugin vive en su propia carpeta con su estructura completa.
